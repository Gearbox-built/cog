#!/bin/bash
#
# Trying to figure out the best way to do this. Should we be doing this with a JSON config file? Or should we be
# sticking true with the bash side of things and keep things in config files. JSON files would be more powerful but also
# probably more confusing. For now, let's leave these both here as examples until we can figure it out.
#
# shellcheck disable=SC2034
{

ADMIN_EMAIL="troy@gearboxbuilt.com"
DEFAULT_PORT="8888"

}